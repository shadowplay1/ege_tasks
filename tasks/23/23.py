def f(x,y):
    #продолжаем если x=y
    if x==y:return 1
    # заканчиваем если x>,а иногда бывает что надо числа не входили.
    # к примеру x==22:return 0
    if x>y:return 0
    # иначе выводим функцию. примерная функция f(x+1,y)+f(x+2,y) ...
    return func
# иногда есть значение которые  должны входить в диапазон.
# к примеру  входят в промежуток err
print(f(start,err)*f(err,end))


# made by isp